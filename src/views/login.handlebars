<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - La Casa del Café</title>
    <link rel="stylesheet" href="/styles/login.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Courier New', Courier, monospace;
            background-color: #f7e7d4;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }
        .login-container {
            background-color: #fff;
            padding: 2em;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 100%;
            max-width: 400px;
        }
        .login-container h1, .login-container h2 {
            margin-bottom: 1em;
        }
        .login-container h1 {
            color: #8b4513;
        }
        .input-group {
            display: flex;
            align-items: center;
            margin-bottom: 1.5em;
            border: 1px solid #ccc;
            border-radius: 4px;
            overflow: hidden;
        }
        .input-group-text {
            background-color: #f7f7f7;
            padding: 0.5em;
            border-right: 1px solid #ccc;
        }
        .form-control {
            border: none;
            padding: 0.75em;
            flex: 1;
        }
        .btn {
            padding: 0.75em 1.5em;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .btn-success {
            background-color: #6b4226;
            color: white;
        }
        .btn-success:hover {
            background-color: #5a361f;
        }
        .btn-primary {
            background-color: #007bff;
            color: white;
        }
        .btn-primary:hover {
            background-color: #0069d9;
        }
        .btn-danger {
            background-color: #dc3545;
            color: white;
        }
        .btn-danger:hover {
            background-color: #c82333;
        }
        .social-login {
            margin-top: 24px;
            padding: 1em;
            border: 1px solid #ccc;
            border-radius: 8px;
            display: flex;
            justify-content: space-around;
        }
        .register-link {
            margin-top: 24px;
            font-size: 0.9em;
        }
        .register-link a {
            color: #8b4513;
            text-decoration: none;
        }
        .register-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1><b>La Casa del Café</b></h1>
        <h2><b>Iniciar Sesión</b></h2>

        <form action="/api/users/pplogin" method="post">
            <div class="input-group">
                <span class="input-group-text" id="group1"><i class="fa-solid fa-user"></i></span>
                <input type="email" name="username" class="form-control" placeholder="Correo Electrónico" required="required" autofocus="autofocus">
            </div>

            <div class="input-group">
                <span class="input-group-text" id="group2"><i class="fa-solid fa-lock"></i></span>
                <input type="password" name="password" class="form-control" placeholder="Contraseña" required="required">
            </div>
            
            <button type="submit" class="btn btn-success">Ingresar</button>
        </form>
        
        <div class="social-login">
            <button class="btn btn-primary" onclick="window.location.replace('/api/users/ghlogin')">
                <i class="fa-brands fa-github"></i> Github
            </button>
            <button class="btn btn-danger" onclick="window.location.replace('/views/login')">
                <i class="fa-brands fa-google"></i> Google
            </button>
        </div>

        <div class="register-link">
            ¿No tienes cuenta? <a href="/views/register">Regístrate</a>
        </div>
    </div>
</body>
</html>
